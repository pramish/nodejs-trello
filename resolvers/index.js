const { trello } = require("../helper");

module.exports = RootValue = {
  createCard: async ({ name }) => {
    try {
      console.log("Am I coming");
    } catch (error) {
      console.log(error);
    }
  },
};
